apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "open-swe.fullname" . }}
  labels:
    {{- include "open-swe.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
